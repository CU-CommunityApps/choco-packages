version: 0.2

phases:
  pre_build:
    commands:
      - New-Item -ItemType Junction -Path C:\ChocoPacker -Value $Env:CODEBUILD_SRC_DIR\build\ChocoPacker
      - cd C:\ChocoPacker
      - nuget.exe restore

  build:
    commands:
      - msbuild
