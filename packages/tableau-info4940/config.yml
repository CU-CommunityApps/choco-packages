Id:             'tableau-info4940'
Description:    'Tableau - INFO4940'
Version:        '2018.1.2'

Install:
    File:       '%INSTALL_DIR%\TableauDesktop-64bit-2018-1-2.exe'
    FileType:   'exe'
    Arguments:  '/quiet /norestart ACCEPTEULA=1 AUTOUPDATE=0 DESKTOPSHORTCUT=0 REGISTER=1 ACTIVATE_KEY=%STATIC_SYSTEM_TABLEAU_ACTIVATE_KEY%'
    ExitCodes:  [0,3010]

Applications:
    tableau:
        DisplayName:    'Tableau'
        Path:           '%windir%\System32\WindowsPowerShell\v1.0\powershell.exe'
        LaunchParams:   '-windowstyle hidden -executionpolicy bypass -file \"%PROGRAMFILES%\Tableau\Tableau 2018.1\bin\scripts\tableau.ps1\"'
        WorkDir:        ''

Registry: 
    HKUD:   
        SOFTWARE\Tableau\Registration\Data:
            (Default):
                Type:   'String'
                Value:  ''

            company:
                Type:   'String'
                Value:  'Cornell'

            country:
                Type:   'String'
                Value:  'US'

            db_annnual_sales:
                Type:   'String'
                Value:  '%TABLEAU_SALES%'

            db_country:
                Type:   'String'
                Value:  'US'

            db_employee_count:
                Type:   'String'
                Value:  '%TABLEAU_EMPLOYEE_COUNT%'

            db_industry:
                Type:   'String'
                Value:  'Healthcare Providers'

            db_sic:
                Type:   'String'
                Value:  '8011'

            db_source:
                Type:   'String'
                Value:  'ip'

            db_state:
                Type:   'String'
                Value:  'NY'

            db_sub_industry:
                Type:   'String'
                Value:  'Clinics'

            db_title:
                Type:   'String'
                Value:  ''

            db_website:
                Type:   'String'
                Value:  'cornell.edu'

            db_zip:
                Type:   'String'
                Value:  '14853'

            department:
                Type:   'String'
                Value:  'IT'

            elqCustomerGUID:
                Type:   'String'
                Value:  '%TABLEAU_ELQ_CUST%'

            email:
                Type:   'String'
                Value:  '%TABLEAU_EMAIL%'

            first_name:
                Type:   'String'
                Value:  '%TABLEAU_FIRST_NAME%'

            form_environment:
                Type:   'String'
                Value:  'qt'

            form_variant:
                Type:   'String'
                Value:  'short'

            ga_client_id:
                Type:   'String'
                Value:  '%TABLEAU_CLIENT_ID%'

            job_role:
                Type:   'String'
                Value:  'Professor/Teacher'

            last_name:
                Type:   'String'
                Value:  '%TABLEAU_LAST_NAME%'

            phone:
                Type:   'String'
                Value:  '%TABLEAU_PHONE%'

            product_name:
                Type:   'String'
                Value:  'desktop'

            state:
                Type:   'String'
                Value:  'NY'

            title:
                Type:   'String'
                Value:  'IT Professor Teacher'

            zip:
                Type:   'String'
                Value:  '14853'

